apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
        - image: gcr.io/sandbox-diamanti-io/kubeadm-control-plane-controller-amd64:v0.3.3.4
          name: manager
